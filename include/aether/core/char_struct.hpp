#pragma once 
#include <vector>
namespace aether::core {
template <int Dim,int numvar>
struct characteristics {
//     std::vector< Mat<numvar> > test;
//     characteristics() = default;
//     characteristics(int )
// };

std::vector<double> x;
std::vector<double> y;
}