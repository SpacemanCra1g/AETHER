#pragma once 
#include <aether/core/config.hpp>

namespace aether::core{
    // I'm continuing the bad habbit of templating on dimension rather than making a specific enum for the sweep
    template<int Dim>
}